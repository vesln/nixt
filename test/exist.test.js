describe('nixt#exist', function() {
  xit('can verify if a file exists');
  xit('returns an error when the file does not exist');
  xit('can verify if a directory exists');
  xit('returns an error when the directory does not exist');
});
